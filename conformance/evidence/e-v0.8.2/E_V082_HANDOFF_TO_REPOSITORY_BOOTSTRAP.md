# E v0.8.2 Handoff to Repository Bootstrap

This handoff becomes actionable only if the Master declares M2 / authorizes bootstrap.

Frozen repository decisions supplied by the user:
- language/project: **Marak**;
- GitHub owner: `Sargon-17-Green`;
- repository: `Marak`;
- initial visibility: private;
- license: MIT;
- CLI: `marak`;
- distribution: `marak`, fallback `marak-lang` only if required at publication time.

Recommended documentation layout:
```text
docs/
  he/
  en/
  llm/
```
The LLM guide is non-normative; it must be derived from the specification/conformance suite and include canonical patterns, negative patterns, anti-hallucination rules and edition-specific constraints.

No repository was created by E.
