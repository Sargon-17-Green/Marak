# Documentation map

- `he/` — human guide in Hebrew.
- `en/` — human guide in English.
- `llm/` — guide optimized for LLM / AI coding agents.
- `history/` — project history and origin.
- `compiler/` — implementation-specific documentation retained from the verified compiler baseline.
- `archive/` — historical milestone material.

Only the normative specification under `spec/` defines the language.
