# C M4 — Handoff to E v0.8

This document is input for a later Master-authorized E v0.8 task; C does not start E here.

E should treat the following as executable C surfaces:

- A13 whole-program check/resolve/validate;
- canonical HAST version `core-hast-0.1-candidate-1`;
- IR `core-ir-0.1-candidate-1`;
- artifact `core-artifact-0.1-candidate-1`;
- IR reference evaluator and portable backend;
- diagnostics listed in the updated catalog;
- `C_M4_A13_B12_CONTRACT.json` mapping.

High-value adversarial families are exact whitespace/U+200B, zero/two `ועתה`, adjacency-as-sequence trap, hoisting, cross-kind same spelling, positional-role trap, output-is-return trap, proposition-is-value trap, completion-is-HALT trap, checked-subtraction failures, stale/dynamic no-output provenance, malformed artifacts and relocation/package independence.
