# C M4.2 Repository Bootstrap Readiness

Canonical source target: future repository `Sargon-17-Green/Marak` (not created by C M4.2).

Current source-tree readiness checks:

- project/language name: Marak;
- distribution metadata: `marak` (fallback `marak-lang` only if publication later requires it);
- CLI entry point: `marak`;
- license: MIT;
- no stale source-root handoff manifest/checksum ledger;
- no old wheel/build payload embedded in source root;
- no temporary handoff input/evidence directories in source root; canonical example artifacts remain as tested fixtures;
- no absolute workspace dependency;
- historical milestone documents explicitly archived;
- tests and packaging are relocatable.

Repository creation, Git history, CI and publishing remain outside this remediation task.
