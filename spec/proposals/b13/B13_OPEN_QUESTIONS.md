# B13 Open Questions

No question blocks B13 semantic-model handoff.

Deferred to A/B integration or later work:
- Biblical surface forms for all seven requests;
- general Text unit/normalization/concatenation;
- generic Integer arithmetic;
- user-defined comparator acts;
- mutable collections;
- heterogeneous product/record values;
- multiple outputs from one act occurrence;
- exception/recovery model;
- concrete host transport for Program Input Roles;
- canonical binary artifact encoding.

Surface-dependent integration choices that do not invalidate the preferred B models:
- how a symbol-domain strict order is stated linguistically;
- exact wording of non-strict ordering propositions;
- whether A14 exposes every new domain as act output immediately or phases them in.
