# B13 — Post-M2 Semantic Expansion for Megillah Requirements

Status: **PROPOSAL / READY FOR A/B INTEGRATION REVIEW**

Baseline: B12 frozen Core v0.1; M2 complete; `main` = `45ac2aebe391f7aa83792e1e501dcd69da884710`.
D2 requests 001..007 establish needs, not pre-approved semantic forms.

Preferred models:
1. finite declared runtime Symbols; general Text deferred;
2. BidirectionalIndex values distinct from Naturals;
3. immutable finite ordered homogeneous Collections, recursively nestable;
4. Natural ordering propositions LT/GT plus derived LE/GE;
5. no new B semantics for direct numerals >9999;
6. exact counted recurrence with count fixed once before iteration;
7. named immutable Program Input Roles bound per invocation before Preparation.

Cross-domain integration extends existing B12 concepts rather than importing variables/functions:
- state-bearing referents have one declared value domain;
- act roles have one declared domain and occurrence-specific immutable values;
- act output remains zero/one, but its declared payload domain may be a B13 value domain.
B12 programs remain the Natural-specialized subset.

No Biblical syntax and no production compiler changes are selected here.
